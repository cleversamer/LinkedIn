import styled from "styled-components";
import Header from "../components/Header";

const Home = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

const Container = styled.div``;

export default Home;
